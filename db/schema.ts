import { pgTable, uuid, varchar, timestamp, integer, text, boolean } from 'drizzle-orm/pg-core';
export const activities=pgTable('activities',{id:uuid('id').defaultRandom().primaryKey(),name:varchar('name',{length:120}).notNull(),location:varchar('location',{length:200}).notNull(),activityDate:timestamp('activity_date').notNull(),isActive:boolean('is_active').default(false).notNull(),personInCharge:varchar('person_in_charge',{length:120}).notNull()});
export const users=pgTable('users',{id:uuid('id').defaultRandom().primaryKey(),name:varchar('name',{length:120}).notNull(),role:varchar('role',{length:20}).notNull(),contact:varchar('contact',{length:30})});
export const patients=pgTable('patients',{id:uuid('id').defaultRandom().primaryKey(),nik:varchar('nik',{length:30}),name:varchar('name',{length:120}).notNull(),age:integer('age').notNull(),birthDate:timestamp('birth_date'),gender:varchar('gender',{length:20}).notNull(),address:text('address'),phone:varchar('phone',{length:30}),mainComplaint:text('main_complaint'),category:varchar('category',{length:40})});
export const visits=pgTable('visits',{id:uuid('id').defaultRandom().primaryKey(),activityId:uuid('activity_id').references(()=>activities.id).notNull(),patientId:uuid('patient_id').references(()=>patients.id).notNull(),queueNumber:varchar('queue_number',{length:10}).notNull(),status:varchar('status',{length:20}).notNull(),priority:varchar('priority',{length:20}).notNull(),createdAt:timestamp('created_at').defaultNow().notNull()});
export const triageRecords=pgTable('triage_records',{id:uuid('id').defaultRandom().primaryKey(),visitId:uuid('visit_id').references(()=>visits.id).notNull(),bloodPressure:varchar('blood_pressure',{length:20}),temperature:varchar('temperature',{length:10}),pulse:integer('pulse'),respiratoryRate:integer('respiratory_rate'),oxygenSaturation:integer('oxygen_saturation'),weight:integer('weight'),notes:text('notes')});
export const medicalRecords=pgTable('medical_records',{id:uuid('id').defaultRandom().primaryKey(),visitId:uuid('visit_id').references(()=>visits.id).notNull(),anamnesis:text('anamnesis'),physicalExam:text('physical_exam'),diagnosis:text('diagnosis'),treatment:text('treatment'),doctorNotes:text('doctor_notes')});
export const medicines=pgTable('medicines',{id:uuid('id').defaultRandom().primaryKey(),name:varchar('name',{length:120}).notNull(),unit:varchar('unit',{length:20}).notNull(),stockInitial:integer('stock_initial').notNull(),stockAvailable:integer('stock_available').notNull(),criticalThreshold:integer('critical_threshold').notNull()});
export const prescriptions=pgTable('prescriptions',{id:uuid('id').defaultRandom().primaryKey(),visitId:uuid('visit_id').references(()=>visits.id).notNull(),notes:text('notes')});
export const prescriptionItems=pgTable('prescription_items',{id:uuid('id').defaultRandom().primaryKey(),prescriptionId:uuid('prescription_id').references(()=>prescriptions.id).notNull(),medicineId:uuid('medicine_id').references(()=>medicines.id).notNull(),qty:integer('qty').notNull(),dosage:varchar('dosage',{length:80})});
export const referrals=pgTable('referrals',{id:uuid('id').defaultRandom().primaryKey(),visitId:uuid('visit_id').references(()=>visits.id).notNull(),facility:text('facility').notNull(),reason:text('reason').notNull(),temporaryDiagnosis:text('temporary_diagnosis'),notes:text('notes'),status:varchar('status',{length:20}).notNull()});
export const documents=pgTable('documents',{id:uuid('id').defaultRandom().primaryKey(),patientId:uuid('patient_id').references(()=>patients.id),visitId:uuid('visit_id').references(()=>visits.id),activityId:uuid('activity_id').references(()=>activities.id),fileName:varchar('file_name',{length:255}).notNull(),fileType:varchar('file_type',{length:80}).notNull(),fileSize:integer('file_size').notNull(),url:text('url').notNull()});
export const auditLogs=pgTable('audit_logs',{id:uuid('id').defaultRandom().primaryKey(),userId:uuid('user_id').references(()=>users.id),action:varchar('action',{length:120}).notNull(),entity:varchar('entity',{length:80}).notNull(),entityId:uuid('entity_id'),createdAt:timestamp('created_at').defaultNow().notNull()});
